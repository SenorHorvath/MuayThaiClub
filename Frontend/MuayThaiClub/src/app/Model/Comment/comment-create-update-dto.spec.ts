import { CommentCreateUpdateDto } from './comment-create-update-dto';

describe('CommentCreateUpdateDto', () => {
  it('should create an instance', () => {
    expect(new CommentCreateUpdateDto()).toBeTruthy();
  });
});
