import { AuthLoginResultDto } from './auth-login-result-dto';

describe('AuthLoginResultDto', () => {
  it('should create an instance', () => {
    expect(new AuthLoginResultDto()).toBeTruthy();
  });
});
