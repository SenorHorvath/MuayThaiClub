import { GetCurrentAuthDto } from './get-current-auth-dto';

describe('GetCurrentAuthDto', () => {
  it('should create an instance', () => {
    expect(new GetCurrentAuthDto()).toBeTruthy();
  });
});
