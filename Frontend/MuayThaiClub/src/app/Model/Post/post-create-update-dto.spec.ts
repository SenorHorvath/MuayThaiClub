import { PostCreateUpdateDto } from './post-create-update-dto';

describe('PostCreateUpdateDto', () => {
  it('should create an instance', () => {
    expect(new PostCreateUpdateDto()).toBeTruthy();
  });
});
