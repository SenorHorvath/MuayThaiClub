import { UserLoginResultDto } from './user-login-result-dto';

describe('UserLoginResultDto', () => {
  it('should create an instance', () => {
    expect(new UserLoginResultDto()).toBeTruthy();
  });
});
