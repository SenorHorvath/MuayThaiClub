import { GetCurrentUserDto } from './get-current-user-dto';

describe('GetCurrentUserDto', () => {
  it('should create an instance', () => {
    expect(new GetCurrentUserDto()).toBeTruthy();
  });
});
